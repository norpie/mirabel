import Root from './chat.svelte';
import Log from './chat-log.svelte';
import Message from './chat-message.svelte';
import TypingIndicator from './chat-typing-indicator.svelte';
import Action from './chat-action.svelte';
import Input from './chat-input.svelte';

export {
    Root,
    Log,
    Message,
    TypingIndicator,
    Action,
    Input,

    Root as Chat,
    Log as ChatLog,
    Message as ChatMessage,
    TypingIndicator as ChatTypingIndicator,
    Action as ChatAction,
    Input as ChatInput,
}